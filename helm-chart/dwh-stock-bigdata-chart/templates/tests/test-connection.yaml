apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dwh-stock-bigdata-chart.fullname" . }}-test-connection"
  labels:
    {{- include "dwh-stock-bigdata-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dwh-stock-bigdata-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
