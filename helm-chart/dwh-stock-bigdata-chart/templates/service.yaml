apiVersion: v1
kind: Service
metadata:
  name: {{ include "dwh-stock-bigdata-chart.fullname" . }}
  labels:
    {{- include "dwh-stock-bigdata-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dwh-stock-bigdata-chart.selectorLabels" . | nindent 4 }}
