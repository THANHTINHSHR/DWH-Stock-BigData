apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "superset-chart.fullname" . }}-test-connection"
  labels:
    {{- include "superset-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "superset-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
