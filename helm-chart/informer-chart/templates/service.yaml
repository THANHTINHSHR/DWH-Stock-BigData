apiVersion: v1
kind: Service
metadata:
  name: {{ include "informer-chart.fullname" . }}
  labels:
    {{- include "informer-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "informer-chart.selectorLabels" . | nindent 4 }}
