apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "informer-chart.fullname" . }}-test-connection"
  labels:
    {{- include "informer-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "informer-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
