apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "influxdb-chart.fullname" . }}-test-connection"
  labels:
    {{- include "influxdb-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "influxdb-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
